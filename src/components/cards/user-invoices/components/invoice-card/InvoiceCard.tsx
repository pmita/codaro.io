import { Card, CardContent, CardFooter, CardHeader, CardTitle } from "@/components/ui/card"


export const InvoiceCard = (data: any) => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>
          {status}
        </CardTitle>
      </CardHeader>
      <CardContent>

      </CardContent>
      <CardFooter>
        
      </CardFooter>
    </Card>
  )
}